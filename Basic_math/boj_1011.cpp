#include <iostream>
using namespace std;

int main(){
    int T;
    long long x, y;
    cin >> T;
    for (int i =0; i<T; i++){
        cin >> x >> y;
        int max = 0;
        int cnt = 0;
        double distance = y-x;
        double dispow = sqrt(distance);
        int pow = round(sqrt(distance));

        if ()
}